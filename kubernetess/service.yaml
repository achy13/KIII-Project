apiVersion: v1
kind: Service
metadata:
  name: projects-service
  namespace: securelogin-namespace
spec:
  selector:
    app: securelogin-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8088